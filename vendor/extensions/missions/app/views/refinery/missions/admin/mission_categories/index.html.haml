%section#records
  = render 'records'
%aside#actions
  = render 'actions'
= render '/refinery/admin/make_sortable', tree: false if !searching? and ::Refinery::Missions::Admin::MissionCategoriesController.sortable? and ::Refinery::Missions::MissionCategory.count > 1

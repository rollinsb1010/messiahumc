.staff_category.category
  %h3= staff_category[:name]
  %ul
    - staff_category[:members].each do |staff_member|
      %li
        = link_to staff_member.name, staff_category[:url].call(staff_member)
        %p{class: 'job_title'}
          = staff_member.job_title

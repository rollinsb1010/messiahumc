%section#records
  = render 'records'
%aside#actions
  = render 'actions'
= render '/refinery/admin/make_sortable', tree: false if !searching? and ::Refinery::Ministries::Admin::MinistryCategoriesController.sortable? and ::Refinery::Ministries::MinistryCategory.count > 1

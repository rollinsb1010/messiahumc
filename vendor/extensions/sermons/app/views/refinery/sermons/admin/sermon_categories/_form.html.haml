= form_for [refinery, :sermons_admin, @sermon_category] do |f|
  = render '/refinery/admin/error_messages', object: @sermon_category, include_object_name: true
  .field
    = f.label :name
    = f.text_field :name, class: 'larger'
  = render '/refinery/admin/form_actions', f: f, continue_editing: false, delete_title: t('delete', scope: 'refinery.sermons.admin.sermons.sermon_category'), delete_confirmation: t('message', scope: 'refinery.admin.delete', title: @sermon_category.name)
- content_for :javascripts do
  :javascript
    $(function(){
    page_options.init(false, '', '');
    });

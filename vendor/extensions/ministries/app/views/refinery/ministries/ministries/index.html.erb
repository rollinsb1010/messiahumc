<% content_for :body_content_left do %>
  <ul id="ministries">
    <% @ministries.each do |ministry| %>
      <li>
        <%= link_to ministry.name, refinery.ministries_ministry_path(ministry) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>

%section#records
  = render 'records'
%aside#actions
  = render 'actions'
= render '/refinery/admin/make_sortable', tree: false if !searching? and ::Refinery::Sermons::Admin::SermonCategoriesController.sortable? and ::Refinery::Sermons::SermonCategory.count > 1

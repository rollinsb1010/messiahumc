%section#records
  = render 'records'
%aside#actions
  = render 'actions'
= render '/refinery/admin/make_sortable', tree: false if !searching? and ::Refinery::Messengers::Admin::MessengersController.sortable? and ::Refinery::Messengers::Messenger.count > 1

= form_for [refinery, :missions_admin, @mission_category] do |f|
  = render '/refinery/admin/error_messages', object: @mission_category, include_object_name: true
  .field
    = f.label :name
    = f.text_field :name, class: 'larger'
  .field
    = f.label :index_placement
    = select :mission_category, :index_placement, left: 'left', right: 'right'
  = render '/refinery/admin/form_actions', f: f, continue_editing: false, delete_title: t('delete', scope: 'refinery.missions.admin.missions.mission_category'), delete_confirmation: t('message', scope: 'refinery.admin.delete', title: @mission_category.name)
- content_for :javascripts do
  :javascript
    $(function(){
    page_options.init(false, '', '');
    });

- content_for :body_content_right do
  .by_category
    %h1 #{@category.name} Ministries
    .ministries
      .left
        = render partial: 'ministry_list', locals: {ministries: @left_ministries}
      .right
        = render partial: 'ministry_list', locals: {ministries: @right_ministries}
      %span.clear

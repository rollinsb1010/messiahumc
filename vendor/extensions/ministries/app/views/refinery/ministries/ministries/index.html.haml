- content_for :body_content_right do
  .ministries_index
    %h1 Highlighted Ministries
    .ministries
      .left
        = render partial: 'ministry_list', locals: {ministries: @left_highlighted_ministries}
      .right
        = render partial: 'ministry_list', locals: {ministries: @right_highlighted_ministries}
      %span.clear

.ministry_category.category
  %h2= ministry_category.name
  %ul
    - if highlighted
      - ministries = ministry_category.highlighted_ministries
    - else
      - ministries = ministry_category.ministries

  = render partial: 'ministry_list', locals: {ministries: ministries}

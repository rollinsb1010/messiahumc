%table.messengers{id: name}
  %thead
    %tr
      %th.published_date Date
      %th Actions
  %tbody
    - messengers.each do |messenger|
      %tr
        %td= messenger.published_at.strftime('%-m/%-d/%Y')
        %td= link_to "Download", messenger.pdf_file.url

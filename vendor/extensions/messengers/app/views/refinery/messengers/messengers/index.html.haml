- content_for :body_content_left do
  = render partial: 'messenger_table', locals: {name: 'weekly', messengers: @weekly_messengers}
  = render partial: 'messenger_table', locals: {name: 'monthly', messengers: @monthly_messengers}

= render '/refinery/content_page'

<% content_for :body_content_left do %>
  <ul id="sermons">
    <% @sermons.each do |sermon| %>
      <li>
        <%= link_to sermon.speaker, sermon_path(sermon) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render :partial => "/shared/content_page" %>
